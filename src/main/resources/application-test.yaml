spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:postgres
    username: test
    password: test
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect